@using WebQuanAoNamNu1.Model;
@{
    ViewBag.Title = "News";
    Layout = "~/Views/Shared/_Layout.cshtml";

}
<section class="header_text sub">
    <h2 style="color:crimson;"><span>Tin tức khuyến mãi</span></h2>
</section>
<hr />
<section class="main-content">
    @foreach (var item in Model)
    {
        @*<div class="row">
                <div class="span4" style="padding-right:120px;">
                    <a href="#" class="thumbnail" data-fancybox-group="group1" title="Description 1"><img alt="" src="@Url.Content(item.Image)" width="400" height="130"></a>

                </div>
                <div class="span5">
                    <address>
                        <h4><strong>@item.Title </strong></h4> <br>
                        <span>@item.Description</span><br>
                    </address>
                </div>
            </div>*@
        <div class="container mt-5 mb-5">
            <div class="row d-flex justify-content-center">
                <div class="col-md-8">
                    <div class="d-flex flex-row"></div>
                    <div class="row news-card p-3 bg-white">
                        <div class="col-md-4">
                            <a href="#" class="thumbnail" data-fancybox-group="group1" title="Description 1"><img alt="" src="@Url.Content(item.Image)" width="400" height="130"></a>

                        </div>
                        <div class="col-md-8">
                            <div class="news-feed-text">
                                <address>
                                    <h3><strong>@item.Title </strong></h3> <br>
                                    <span><h4>@item.Description</h4></span><br>
                                </address>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    }
</section>

