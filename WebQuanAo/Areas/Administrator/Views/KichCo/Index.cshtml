@model IEnumerable<WebShop.Model.KichCo>

@{
    ViewBag.Title = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenKichCo)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenKichCo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MaKichCo }) |
                @Html.ActionLink("Details", "Details", new { id=item.MaKichCo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MaKichCo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
